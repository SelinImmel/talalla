<div class="container">
  <h2>Show studio dashboard</h2>

  <p>Upcoming events</p>
  <%#= Event.where(params[:studio_id]).each do |e| %>
  <% @events.each do |e| %>
  <%= e.title %>
  <%= e.date %>
  <%= e.content %>
  <p>link to the show of event</p>
  <% end %>

</div>
