<%= simple_form_for [post, Comment.new] do |f| %>
  <%= f.input :content, label: false, placeholder: 'Type your comment here...' %>
  <%= f.submit 'comment', class: "btn btn-outline-primary btn-sm" %>
<% end %>
