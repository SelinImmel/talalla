<div class="container">
  <h2>Add a new dedicated channel</h2>
  <%= simple_form_for @category do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.button :submit, class: "btn btn-primary" %>
  <% end %>
</div>
