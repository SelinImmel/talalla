
<div class="container">
  <% @students.each do|student|%>
    <p><strong><%= student.first_name%> <%= student.last_name%> </strong>
    <br><%= student.email%>
    <br><%= student.address%>
    <br><%= student.biography %>
  </p>
  <% end %>
  <%= link_to "Back", admin_path %>
</div>
