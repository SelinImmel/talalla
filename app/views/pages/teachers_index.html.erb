<div class="container">
  <% @teachers.each do|teacher|%>
  <p><strong><%= teacher.first_name%> <%= teacher.last_name%> </strong>
    <br><%= teacher.email%>
    <br><%= teacher.address%>
    <br><%= teacher.biography %>
  </p>
  <% end %>
  <%= link_to "Back", admin_path %>
</div>
