
<br>

<div class="container">
  <% @subscriptions.each do|subscription|%>
   <p><strong><%= subscription.name%></strong>
    <br><%= subscription.content%>
    <br>Number of lessons: <%= subscription.package_size%>
    <br><%= subscription.durability%>
    <br><%= subscription.amount%> IDR <%= link_to "Delete" %>
    </p>
  <% end %>

  <%= link_to "Back", admin_path %>
</div>

