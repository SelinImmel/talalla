document.querySelector(`#allComment${<%= @post.id %>}`).innerHTML = "<%= j render partial: 'studios/comments', locals: { comments: @post.comments.last(2)} %>"
document.querySelector("#see-comment-options<%= @post.id %>").innerHTML = "<%= j link_to 'see all comments...', show_all_comments_post_path(@post), remote: true %>"
